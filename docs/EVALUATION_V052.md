# 第 0 轮与第 30 轮冻结评测

后续联合分析议题：用户于2026-09-22要求记住“种子依赖、精英独占繁殖、重启及历史候选探索”讨论，待本轮结束后结合结果分析。详见 [项目记忆](SEARCH_EXPLORATION_MEMORY_20260922.md)。这是待检验问题与备选实验，不改变本轮冻结协议。

2026-09-22 用户明确授权：修复检查点兼容读取，执行训练前后评测并报告对比结果。

源实验只读保留：`NicheFlow_Revision_v051/runs/main_v051_local_flash_r30`。新代码部署到独立 `NicheFlow_Evaluation_v052`，不覆盖源目录。

兼容范围：统一导出字符串生态位键；旧摘要仅在内存还原整数键后逐字核对，并要求检查点与完整哈希日志快照一致。代码迁移通过完整来源／目标文件哈希清单，限定为序列化和独立评测支持，禁止改变路由、模型、执行器、特征与训练算法文件。原文件及原摘要保留。

评测固定为完整 500 题 MATH-500，单次采样。第 0 轮指完成初始工作流开发集评价、尚未开始 30 轮学习的状态；不是现在重新训练一个初始系统。第 30 轮为已完成状态。两者保留三组原权重 `[0.5,0.5]`、`[0.25,0.75]`、`[0.75,0.25]`，使用同一个冻结 OFUL/Chebyshev 规则；另加初始 Qwen、Flash 单模型基线，共 8 个结果组。

所有路由仅使用题目公开输入，在生成任何测试答案前固定。按原规则保留不确定性项，不因测试表现修改路由或提示。两模型、温度、2048-token 上限、截断记 0 分和本地费用计 0 均不变。没有学习更新、搜索或新增轮数。

同一道题、完全相同工作流与相同样本号只执行一次，各组共享同次结果；执行 seed 来自固定题号、工作流 ID 和原 decode seed。采用事先固定的逐题交错顺序。该方案减少重复付费，使相同选择得到相同答案；它不是多个独立随机种子的重复实验。Flash 端不支持固定 seed，所有复用均依赖真实回执，不重新生成。

分别报告物理支出和各组独立使用时的逻辑成本。各组每题费用包含该工作流全部真实节点调用，不能按共享组数打折。同时提供按冻结非高峰、未缓存 API 单价重新计价的参照费用，以减少执行时间与缓存命中不同造成的比较偏差；这不改变训练奖励或原账本。工作流耗时为节点调用耗时之和，路由预计算和加载耗时另报。

全评测 API 上限沿用独立终评配置的 15 美元、时间上限 24 小时。调用数上限根据所有实际选中的唯一工作流计划计算。结果逐条写入哈希日志；对未知计费结果不自动重试。评测完成后核对源日志与首末检查点未变。

报告每组正确率、每题费用、工作流调用耗时、截断率，并对三个前后配对报告提升百分点、配对 bootstrap 区间与未作多重校正的精确 McNemar p 值。单次采样不证明跨随机种子稳定性。开始终评后，不再用该测试集判断是否追加学习。
