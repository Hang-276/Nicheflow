# NicheFlow 工程记录

## 当前：0.4.4 候选契约修复与6轮准备（2026-09-21）

用户要求修复候选生成，为6轮实验准备，链路测试通过后停止，不启动正式6轮。实际缺陷不是模型别名不匹配，而是生成器把Format误认为确定性节点，填model=null。原始两份生成输出保留于v043事件链，拒绝本身正确。修复提示逐算子明确执行类型、模型profile枚举、输入数量；Format仍真实调用所选模型；不自动修补null/别名、不取消门禁。报错包含节点/算子/错误值。

tmux入口支持quick/full及显式正整数轮数；准备命令为bash scripts/start_main_tmux.sh quick 6，会话nicheflow-main-v044-quick-r6，目录runs/main_v044_quick_r6。本次不执行。6轮学习5589调用/16小时/15 USD API，终评1260调用/2小时/2 USD API，总6849调用/18小时/17 USD API，均为硬上限。初始化一次、12资源块、7份参数快照、最后仅105次终评；数据/模型/解码/三层机制不变。

本地114项CPU测试通过，包括真实35/70/35数据上6轮构造后端、Format异构执行/档案决策、数值健康、冻结终评、完成复查无新增调用；脚本测试保证rounds=6真正传到驱动而非仍硬编码1，拒绝非法轮数。代码/文档旧版备份.implementation_backups/v043_before_generation_fix.tgz。服务器114/114 CPU测试通过（23.309秒），97个内容文件发布哈希一致；6轮main-plan就绪，0配置问题。

真实链路检查passed=true：55次调用/16次工作流，288.4189秒，API核算0.017601312 USD，0截断、0系统失败、无未知调用/费用。两次生成均合法，代理选中的6调用节点候选在2道开发题上真实评价后被准入（cell86），随后获得新精英路由反馈；候选没有被手工指定模型或强制入库。两份快照有限正定、最大条件数3.647862909177896。正式6轮未启动，runs/main_v044_quick_r6不存在，GPU已回到0 MiB/0%。

服务器证据runs/candidate_chain_v044/，本地副本reports/candidate_chain_v044/；tmux会话nicheflow-candidate-check-v044保留退出现场。既有nicheflow-v043定时监控保持暂停，没有创建新的监控/正式实验。下一步仅等待用户正式启动6轮指令。

真实链路检查限定开发前2题/校准前2题，生产驱动1轮、每Deploy块2查询、无终评，最多82调用/30分钟/1 USD API；新增scripts/check_candidate_chain.py明确要求--execute，已有目录禁止重跑。接受条件为合法生成、生成候选真实执行、档案决策、周期完成、参数健康、计费完整，不能强制精英入库。旧v042/v043运行全部保留。


## 0.4.3 修复记录（2026-09-20）

用户授权“自行修复，修复后直接跑，稳定后停止连续输出并定时监控”。服务器v042真实运行86次调用全部有收据，46个执行，在bootstrap:1:task:10遭遇length截断后错误地整批停止，0学习轮。旧目录和tmux保留。API凭据现已由服务器已有配置提供，未读取或输出密钥值。

修复仅将显式length结束视为有界失败样本，质量0，成本保留，无请求重试、下游生成或残缺答案解析；开发/代理/部署/终评均保留失败，其他执行及计费异常仍停止。配置新增length_limit_outcome；GraphExecutor、SearchService、DeploymentService、MainPolicy反馈、主驱动结束判定和报告一致处理。无改动导师核心公式、数据、模型、2048输出上限或预算。为研究记录公开这一失败处理补全，不冒充导师规定。新版本0.4.3，新目录runs/main_v043_quick_r1；不复用旧日志绕过冻结哈希。

本地111项CPU测试通过，含连续length完成全链路、零分/费用/失败率/无重复调用、非length依然停止。日志reports/tests_length_fix_local.txt。简化一轮仍35题×3权重=105终评，初始化后/轮后参数快照必须健康；完整入口保留。截断单列，不要求零截断或把完成当模型有效。服务器发布与启动结果如下。
服务器发布已完成：95个内容文件哈希一致，111/111 CPU测试通过（20.836秒），无依赖安装。既有API凭据启动门禁通过。按用户授权启动`nicheflow-main-v043-quick-r1`，旧v042现场保留。2026-09-20 22:28:57至22:31:21（北京时间）两次只读观察，已完成调用2→12、事件14→54，进程活动，系统失败和未知费用均0；另1请求正常在途。仍处初始化，尚不能称修复后完整一轮已通过。

已创建线程定时监控`nicheflow-v043`，每5分钟只读观察，有异常/完成才通知，结束后暂停监控。连续输出停止，实验继续后台执行。证据`reports/length_fix_v043_launch_verification.json`，监控快照`reports/monitor_main_v043.jsonl`；禁止自动追加实验或改预算。



## 当前交付：0.4.2 简化终评与保留完整入口（2026-09-20）

用户要求缩短评测、保留完整版调用。本版只实现和进行CPU/构造后端测试，没有调用真实模型或启动第一轮。0.4.1以下记录为历史，当前默认入口与验收以本节及README为准。

### 范围和实现

| 文件 | 变更与边界 |
|---|---|
| `configs/main_math.json` | 默认改为math500_quick35；35题×3权重×1采样=105终评工作流；评测2小时/2 USD API上限 |
| `configs/main_math_full.json` | 原0.4.1完整配置逐字保留；500题/1500槽位、24小时/15 USD API不变，协议摘要仍为0210b0dd455e8ffb6a16db52326f13a13a891c03c2e3124783c59032a312dbcc |
| `data/main_math500_v1/quick35_selection.json` | 新增独立选择清单，不修改原父数据或manifest；冻结ID、原序号、种子、父文件哈希 |
| `math_protocol.py` / `main_config.py` | 七科×五难度各1题，以固定seed及ID哈希排名选题；校验清单并重新计算选择；原500题防缩小门禁保留 |
| `main_driver.py` / `main_reporting.py` | 子集使用父集合序号，避免改变路由置信输入和执行ID派生seed；按实际105/1500槽位核验；报告显式标注诊断子集 |
| `start_main_tmux.sh` / `run_main_once.sh` | 接受quick/full，不带参数默认quick；分开配置/目录/会话，已有会话/目录或已知活动主实验则停止；不自动续跑/切full |
| `test_quick_evaluation.py` | 5项新增回归，其中两套生产配置完整构造运行比较学习状态、共享题路由及收据一致性；清单防篡改、同协议跨轮数、完成复查零新增调用 |

训练35/70分区、模型与解码、三层核心、35题上六种种子初始化、每轮两个资源块、学习1369调用/6小时/5 USD API不变。quick总上限2629调用/8小时/7 USD API；full总上限19369调用/30小时/20 USD API。不是预计花费；内部成本预留131.45/968.45 USD仍不等于付款额。

35题全部来自既有500题父集合；每科5题、每难度7题，其中7题含原始Asymptote源码。选择只用ID/科目/难度，不看答案或模型表现。清单SHA256为ecb3cc7b92662f2641ba61913715a6110bb873190781db949d82533002cb953a；quick协议摘要为5a8449138e185b7a190b24e3733caa43a4808ca5e70f082e14e9789f8783818a。父数据、官方评分及完整配置没有变更。

### 验证与发布

本地109/109 CPU测试通过，日志`reports/tests_quick35_local.txt`。其中quick/full均通过同一生产入口完成一个学习轮次及全部配置评测。构造后端下，除配置摘要外学习状态相同，共享题路由选择和模型收据相同；评测不更新，参数有限/正定，重复读取不新建后端或新增调用。shell入口语法检查通过。全为零真实模型调用，不代表真实模型效果或速度。

服务器同步和CPU验收完成：94个内容文件哈希全部一致，109/109测试通过（19.862秒），未安装或升级依赖。服务器备份为`.implementation_backups/before_v042_20260920.tgz`；实际同步95文件含清单。初次部署命令因SSH默认目录下没有相对路径`.venv/bin/python`而退出127，未执行任何替换；改用已有项目解释器绝对路径后完成部署。不是实验失败，未重跑模型。

quick/full两套零调用main-plan均只报告缺少`DEEPSEEK_API_KEY`，没有其他启动问题；凭据未读出/保存。服务器没有新增主实验目录，不操作已有smoke会话。真实第一轮仍未启动。最终汇总`reports/quick35_v042_verification.json`；服务器完整测试日志`reports/tests_quick35_server.txt`。

另保留本地真实数据/构造后端的简化演练于`reports/math500_quick35_offline/`：358个工作流（含105终评）、898次合成调用、2个健康参数快照，冻结评测不更新，哈希链有效，真实调用0。898不是未来真实用量预测，也不能用该模拟运行速度估计耗时。

本地备份`.implementation_backups/v041_before_quick35_20260920.tgz`；源数据、原Word及所有历史运行不覆盖。服务器仅同步源码/配置/测试/数据/README，本工程记录和验证计划仅留本地。

---

## 历史交付：0.4.1 完整MATH-500终评（2026-09-20）

用户确认接入完整MATH-500，要求做好测试并停在可启动第一轮的位置。本版不运行真实模型、不做API健康调用，也不启动第一轮或正式多轮；此前“评估暂缓”的状态由本节取代。

### 协议与数据产物

- 固定MATH-500 revision `6e4ed1a2a79af7d8630a6b768ec859cb5af4d3be`，完整500条、原顺序、原ID和answer，无评估排除。源文件SHA256 `35dc41080a3680858b27fa7e0533d2d547825316fc5dafe5d316f4ccc5a06132`。
- 学习来源固定为原MATH七科全部7500条train文件，revision `21a5633873b6a120296cce3e2df9d5550074f4a3`。按七科×五难度各取1道开发、2道校准，合计35/70；规模是工程配置，不冒充完整训练集或导师给定数值。
- 学习候选池有49条排除：42条旧题/测试重合、4条缺少可解析非空框内参考答案、1条重复、2条难度未注明。所有原因写入manifest，未依据模型表现排除。终评排除数0。
- 采用明确的文本协议`math_verbatim_with_inline_asymptote_v1`：保留42道终评图形题的原始Asymptote源码，作为文本输入；不删除、不渲染、不执行。该协议不代表视觉或一般附件能力。开发/校准另有3/4道此类题。
- 数据包`data/main_math500_v1/`固定任务和分区哈希；原始文件/下载URL/哈希在`data/source_math_v041/`。PyArrow21.0.0只安装在本地临时目录用于离线读Parquet；运行时不依赖它，服务器模型环境未改动。

### 实现变更与边界

| 位置 | 本版变更 |
|---|---|
| `math_protocol.py` | 全500题原样适配；明确文本Asymptote协议；七科×五难度分层；规范化问题文本去重；参考答案与solution内框中答案一致性检查 |
| `scripts/prepare_math500.py` | 仅从已下载固定revision文件准备，校验源哈希和7500/500数量；目标已存在则拒绝覆盖 |
| `main_config.py` | 500题完整协议不可被静默缩小/暂缓；输入协议检查；跨分区重复不受元数据变化影响；学习与终评分别推导资源上限 |
| `main_budget.py` | 费用累计区分学习/终评；互不借用API额度；独立阶段时间并在恢复时保留；进入新阶段不继承上一阶段临近超时导致的过短单调用超时 |
| `main_driver.py` | 原样使用冻结双头路由完成500×3×1；每25个工作流记录/显示终评进度；不新增更新路径 |
| `main_reporting.py` | 校验完整任务×权重×采样笛卡尔积、任务成员及无终评学习事件；输出各权重失败率、API/本地成本；区分模拟后端和真实数据，防止合成分数被当成实测 |
| `cli.py` / 启动脚本 | main-plan缺少凭据时返回3；tmux启动前完成零调用检查，缺失时不占用唯一运行目录；新运行名v041 |
| `configure_api_key.sh` | 提供用户在自己的SSH终端隐藏输入密钥的入口；只保存0600文件，不调用API、不自动执行实验、不覆盖已有密钥；实现者本轮未执行此入口 |
| `verify_release.py` | 只读核验发布哈希，不导入模型后端 |
| `test_math500.py` | 8项新增回归：500题/42段图形完整保留、35层学习覆盖、完整协议防缩小、轮数不改终评、独立费用/时钟、漏槽位拒绝、跨元数据重题拒绝 |

本次一轮资源上限：学习1369调用/6小时/5 USD API，终评18000调用/24小时/15 USD API，总计19369调用/30小时/20 USD API。终评实际任务数固定1500个工作流，模型调用数随路由图变化。这些是硬停止上限，不是实测或预计花费。内部968.45 USD核算预留是单调用最坏上界之和，同时受更紧的时间/API门禁约束，不是付款预算。正式多轮仅增长学习轮数和对应学习额度，终评不随轮数增长。

### 验证与当前状态

本地104/104项CPU测试通过，日志`reports/tests_math500_local.txt`。真实数据+合成后端的完整一轮在`reports/math500_full_offline/`：1753个工作流（其中1500终评）、5083个合成调用；两份参数快照有限/正定，哈希链有效，终评前后学习状态不变；已完成运行再读新增调用0。该次合成轨迹覆盖了新精英路由反馈，但不能作为真实模型机制证据。测试没有将gold提供给合成后端，它只按公开提示返回固定构造输出。

本版还没有真实一轮模型输出。SSH已恢复；服务器同步、104项服务器CPU回归和发布清单结果在下方最终交付核验更新。API凭据当前未配置；此前自动审批拒绝接收/远程保存密钥的限制没有被绕过，没有读取、传输或保存密钥。

上一版源码/文档已归档至`.implementation_backups/v040_before_math500_20260920.tgz`；旧数据、旧真实运行、原Word均保留。只同步README、代码、配置、脚本、测试和数据；本记录/验证计划仅本地。

### 最终交付核验

0.4.1已同步至既有服务器项目。发布包含清单共92个文件，其中91个内容文件逐一哈希核验通过；服务器旧代码在`.implementation_backups/before_v041_20260920.tgz`备份。首次服务器测试误用了基础Python，两个非MATH评分器因缺少scipy/ujson报错；改用既有项目`.venv/bin/python`后104/104通过，没有安装/升级任何服务器依赖。首次失败记录和最终通过记录均保留。

汇总：`reports/math500_v041_verification.json`；服务器测试：`reports/tests_math500_server.txt`；最终配置检查：`reports/main_plan_v041_server.json`。检查未加载模型、调用数0，唯一剩余问题为缺少`DEEPSEEK_API_KEY`。因此代码/数据部署已完成，但此时`ready_for_configured_run=false`，不能声称凭据已齐全。

已确认服务器`runs/main_v041_r1`和`nicheflow-main-v041-r1`会话均不存在；第一轮未启动。用户可在SSH终端运行`bash scripts/configure_api_key.sh`自行隐藏输入，或明确授权实现者隐藏输入并远程保存；不会把提供脚本本身当成密钥已配置。密钥配置后只做零推理门禁复核并停止，真实第一轮仍由用户决定启动。

---

## 历史交付：0.4.0 主方法驱动（2026-09-20）

本节保留0.4.0当时状态；评估暂缓等描述已由上方0.4.1取代，不作为当前指令。

### 改动责任与接口

| 文件 | 责任及输入/输出 |
|---|---|
| `main_config.py` | 校验主配置；载入含哈希的 development/calibration/evaluation 分区；跨区 ID/公开输入去重；推导轮数对应资源上限；计算排除轮数后的协议摘要 |
| `main_entry.py` | 零调用 `main-plan`、凭据/环境门禁、创建后端、驱动、关闭资源；完成运行无需加载模型；保留已完成失败的退出码 |
| `main_driver.py` | CFG 初始化、任意正整数轮数、每轮实际 Search/Deploy、阶段预算、参数快照、冻结状态、可选独立评估；同一代码运行一轮与多轮 |
| `main_policy.py` | 复用已验证三层实现，绑定实际异构模型及核算成本；导出档案/图/MLP/双头/元头/曲率/统计；矩阵健康与描述性参数变化 |
| `main_models.py` | 本地 Qwen 进程与官方 DeepSeek API；明确有效解码、模型返回身份、缓存/时段费率、未知用量与无重试；密钥仅来自指定环境或0600文件 |
| `main_budget.py` | 操作块资源预留和调用前 API 限额；增量重建费用索引；未知计费禁止继续；实际费用与未使用预留分开 |
| `main_reporting.py` | 从哈希链生成调用/费用/覆盖/健康报告；冻结评估按权重汇总 quality/cost/Pass@k/HV；暂缓状态不伪造测试成绩 |
| `policy.py` / `loops.py` | 增加小范围策略构造接口和生成模型选择；旧 smoke 仍用旧配置/驱动，未替换核心公式 |
| `ledger.py` | O(1) 事件查找、计数缓存；JSON规范化后再哈希并复制载荷，避免整数键排序和历史对象被修改造成哈希不一致 |
| `cli.py` / `reporting.py` | 新增 `--mode main`、主配置只读计划、`--rounds` 与报告分派；非主模式不接受轮数覆盖 |
| `scripts/*main*.sh` | 单次一轮 tmux、保留现场/退出码；重复目录或会话拒绝启动，无自动重试或正式轮数启动 |
| `tests/test_main.py` | 同一生产驱动的合成端到端、评估隔离、超过旧smoke上限、多点中断恢复、预算/费用/参数/失败状态回归 |

状态流：固定学习分区→种子真实评价→循环 Search/Deploy→每轮快照→冻结 `trained_state.json`→按配置评估或明确暂缓。快照包含全部已学习参数，可构造独立冻结路由器；故障恢复以权威事件顺序重放为准，不把单个快照当成足以绕过收据审计的续跑许可。

### 固定协议与新增工程取舍

1. 强档建议使用 DeepSeek Flash，轻量档复用本地 Qwen；不用昂贵闭源模型作为本次门槛。强档 non-thinking、输出2048；API不发送未承诺的seed，记录实际有效参数与别名返回身份。当前未实测强弱优势。
2. 六种CFG模型分配为首个纯轻量，其他图按确定规则分配两个档位；异构占比使用非廉价模型节点/全部节点。100格保持，不要求填满。
3. 主实验采用每查询实测本地推理时间与API用量费率之和。API实际账单、本地租赁账单均未获取；本地1 USD/h仅声明核算率，不能把本地算作免费。
4. 预算块是等最坏资源上界，实际支出不同。每轮2块、部署块8查询、k2、候选12节点、筛选50%；保留原顶层h×搜索UCB归一化和累计余量舍入。未把原文缺失的精确成本分配问题说成已经证明解决。
5. 数据为此前三科各100条train响应内预先固定6开发+24校准；去掉旧题、图形和截断，不按答案形式筛选。来源范围、revision标识与字节哈希明确，不伪装完整MATH或新的全量固定revision下载。
6. 用户要求暂不选择真实评估范围；配置为deferred。代码有独立evaluation分区、冻结OFUL/Chebyshev选择（代码名`frozen_oful_chebyshev`）、多次采样、Pass@k/HV和状态不变校验，实际数据未接。
7. `formal_rounds=100`只是显式占位，驱动只读`rounds`/命令覆盖。正式协议冻结后可仅改轮数运行；当前评估待定是明确例外，未承诺正式研究参数已全部定稿。

### 发现的缺陷与修复证据

异构模型使生态位整数键覆盖更多两位数，离线联调发现事件哈希在写入前与读回后不一致：整数键3、20按数值排序，转为JSON字符串键后按字符排序。已在append入口先转成JSON值再计算哈希；同时复制可变载荷，防止历史状态被后续更新。测试验证整数键读回一致和历史列表不被修改，旧v030事件保持原样。

另修复主实验完成后的两个状态问题：预算失败运行的重复读取不能变成退出码0；即使真实评估暂缓，学习阶段出现的执行错误仍须返回异常状态/码。终端评估每次执行后检查未知用量/费用，不能将未知API支出当零。没有用降低验证要求来绕过问题。

### 本次验证与交付状态

- 本地 **96/96** 项离线测试通过（`reports/tests_main_local.txt`），零真实模型调用。79项历史测试保留，新增17项主实验测试，部分包含多个故障点子测试。
- 合成一轮完整评估、七轮学习、多个持久化边界中断重放均由同一主驱动完成；合成端到端仅是工程证据，不能充作GPU/API测试或主实验结果。
- 数据/代码/README发布清单用于服务器副本核验；服务器同步和真实运行状态另见本节末的交付结论，不能从代码存在推断已部署。
- 真实混合模型一轮尚未执行，新增真实调用0。API密钥接收及远程持久保存被自动审批拦截，理由是具体敏感凭据传输/保存尚未明确授权；没有接收或保存密钥，等待用户授权或自行配置。
- SSH连接已失效，按用户先前许可弹出隐藏密码框恢复连接；连接恢复前不声称服务器已更新。

原0.3.0源码、配置、测试和三份文档已在修改前归档：`.implementation_backups/v030_before_main_20260920.tgz`。旧运行、旧数据、Word均不覆盖。服务器只同步代码/配置/脚本/测试/README及新学习数据；本记录和验证计划只保留本地。

### 正式运行前仍需确认

真实API凭据和有界一轮实测；真实评估范围/固定划分；最终轮数与总预算；对公开工程补全的研究解释。基线/消融不是本方法入口的门槛。代码任务隔离环境、附件/外部工具及另外九项数据的实际准备只在扩展到相应实验时要求，不冒充已覆盖。

### 本地交付结论

三份文档与0.4.0源码完成；本地发布清单74个文件的哈希一致，三个启动/入口脚本语法检查通过。服务器包为`.implementation_backups/main_v040_server_payload.tgz`，不含两份本地专用文档、旧运行或密钥。汇总为`reports/main_v040_verification.json`。

保留一份可复核的离线端到端运行：`reports/main_v040_offline/run/`，synthetic=true，1轮、115次合成调用、2个健康快照、哈希链有效、冻结评估状态未变。该次自然未出现“新精英路由反馈”，coverage明确为false，不强行凑覆盖。此处115不是任何真实模型/API用量，也不是真实一轮预计调用数。多轮/故障情形另在96项测试中验证。

截止交付，SSH认证待用户输入，0.4.0尚未同步或在服务器测试；API凭据授权待回应，真实混合一轮尚未启动。不能称服务器已经就绪，更不能称正式主实验完成。连接/凭据恢复后的必要工作仅为同步和服务器CPU验证、按已授权边界运行一轮并审计后停止；正式轮数仍等待用户另外指令。

---

# 历史记录：NicheFlow 0.3.0

日期：2026-09-20。本地路径 `/Users/yuzihang/Documents/NicheFlow`；服务器项目 `/root/autodl-tmp/NicheFlow_Server_Handoff/NicheFlow_Minimal_Probe`。

## 1. 状态与修复原因

0.2.0只完成组件及两次局部联调，4次真实调用不等于4轮完整实验。其full-smoke入口无条件返回阻塞，未接主循环；将它交给Claude启动是交付错误。此次没有把该阻塞说成导师idea被实验否定，也没有改写历史结果。

0.3.0已经接通完整入口与生产策略，使用同一入口和合成后端验证4周期及恢复。真实GPU smoke仍交由Claude按README启动，实现者没有新增真实调用。

主文档SHA256为`8fae9e00ad672773c63c7e7f382b8f5775d4d7933c9a0a94e622c5f0540766f9`。本地文档中未找到3.4缺失的具体预算分配公式；3.3含线性元老虎机的旧算法及旧证明，不能当成3.4新动作公式已经明确。

用户此次明确授权“没有就标注文档，自己找最小改动替代”。具体补全、原文对应和限制均写在README第6节；不再把普通工程配置、未写完的代码和缺少的理论定义混称为七项spec gap。

## 2. 代码责任划分

| 文件 | 本次职责/改动 |
|---|---|
| policy.py（新增） | 唯一工程补全绑定：CFG、描述符、分箱、代理、搜索反馈、双头路由特征/权重、曲率观测、线性元调度和比例舍入 |
| smoke.py（新增） | 初始化、4周期操作计划、跨层反馈、覆盖判定、预算/错误停止、按收据重放恢复 |
| archive.py | 显式Pareto准入；按原评价顺序重建动态分位数档案，不复制工作流/收据 |
| search.py | 新增来源可追溯的质量加权RBF；保留原组合UCB+logdet(K)与密度筛选 |
| loops.py | 先生成整批再代理排序；真实评价、每查询成本、档案与新增臂；不再逐候选冒充批次百分位筛选 |
| runtime.py/graph.py | 生成种子按attempt_id确定；冻结生成语法；修复超节点/禁用算子校验失败后图对象仍被当成valid的问题 |
| router.py | 每个臂使用自己的OFUL半径；保持双头独立、Chebyshev和幂等更新 |
| scheduler.py | 原曲率估计和回退保留，显式增加cap_at_one边界模式；旧error模式仍可测试 |
| cli.py/spec.py | 删除无条件阻塞，检查真实配置并实例化MinimalPolicy；完成后复查不加载GPU；未就绪配置保存preflight证据 |
| reporting.py | 未创建目录有明确状态；不将无日志推断成零调用；合成运行不能报真实通过；新增曲率/重分箱/反馈投影 |
| start_smoke_tmux.sh/run_smoke_once.sh | tmux单次启动、保留退出现场、console和exit_code；拒绝会话/目录重复启动 |

所有核心补全集中在policy/config，执行、日志、评分组件不隐含调度选择。新增版本0.3.0，无需升级原GPU依赖。

## 3. 关键工程选择及边界

- CFG六种产生式是原语的具体工程实例，并非声称导师提供了六段提示词。
- 空档案先做真实评价，n/q只来自成功的实际评价；动态重分箱会重建按生态位归属的统计。对同一版本的重复生成仍计生成费，但不重复付费评价/增加种群或样本量。
- 成本以实测调用秒数及固定参考费率计算，USD字段是参考标尺。实际租赁账单、模型加载和空闲费用没有伪装成已经测得。
- 两类元奖励保留符号，Search为ΔVendi/100，Deploy为已观测前沿ΔHV。上置信得分只在分配时取非负部分，不改写真实反馈。
- 新增的分配规则为h乘搜索侧UCB后两侧归一化。它保留两个上下文线性模型、真实更新、曲率和反馈，并非原定理推导结果。
- 曲率使用固定快照上的二维HV成对边际，窗口样本和效用定义可审计；重复任务及重叠快照的统计独立性不保证，不能宣称置信界已证明。
- 三层核心smoke采用MATH/单模型/受限LLM图语法；MBPP等代码、异构、附件、工具链依然是明确未覆盖项。

原始gold只在执行后的评分器使用；实际分数可用于开发期学习。查询特征只读取Task.model_input和公开图结构，代理不读取未评价的真实答案或未来分数。

## 4. 恢复、预算与故障处理

每个运行冻结源码、数据、配置和driver contract；改动后不冒充旧版本续跑。Journal仍为独占写锁和追加哈希链。调用开始先落盘；未知已发出请求不能自动补发。

恢复创建新服务状态，以确定顺序重放初始化、评价、分箱、代理更新、搜索统计、路由和元反馈。历史决策使用当时的上下文及剩余工程额度；已完成调用直接复用。上下文或事件不一致立即报冲突。

新初始化和Search块先检查完整最坏调用上界；不足停止。候选节点数已知后的执行也预留整个评价批次。完成记录阻止结束后再次发起调用；墙钟不重置。

连续三次工作流执行失败停止；错误答案不算执行失败。非法生成保留原始输出，不展开包装、补节点或替换答案凑成功。代码/工具任务缺少隔离环境时在模型调用前报告阻塞。

## 5. 验证证据

本轮79项无真实模型调用测试通过，保存于`reports/tests_smoke_repair_local.txt`；发布核验汇总保存于`reports/smoke_release_verification.json`。服务器新代码的CPU验证单列，不重测模型环境。

新增测试涵盖：

- 用实际完整CLI及生产策略跑4个合成周期，确认入口确实加载所选后端并驱动三层。
- 初始统计、动态分箱、批次筛选、真实收据归属、每查询成本、模型异构轴仍为0。
- h改变会改变实际分配；双线性模型接受带符号实测反馈。
- call_finished、execution、archive、rebin、curvature、feedback、scheduler、router、cycle_finished等持久化边界中断后，决策/事件内容与不中断运行一致，无重复调用。
- 新精英未自然出现时报告partial，不强制注入；未知调用停止；已结束后即使墙钟到期也只返回证据。
- 算子/节点限制、坏配置预加载阻止、预算不足不先消费生成额度、三次失败停止。

这些是合成检查，不是4轮真实模型实验。既有4次真实组件联调额度已用完，本次没有偷偷追加健康、暖机或调试调用。

## 6. 发布与保留

修改前备份：`.implementation_backups/before_smoke_repair_20260920.tgz`。更早的0.2.0源码、两次真实联调及旧probe结果均保持原样。

`configs/release_manifest.json`记录本版源码、测试、配置、脚本与README哈希。服务器同步代码/配置/测试/README，本文件及VALIDATION_PLAN仅本地保留。服务器端不覆盖旧runs、数据、环境、模型权重或Claude前次失败现场。

监控入口改为`nicheflow-smoke-v030`和`runs/smoke_v030`。本次不替Claude启动真实smoke，也不设置重复实验或自动重试。后续任何结果必须与本版源码哈希和“工程补全”标签一起报告。

发布完成：本地与服务器均79/79通过；服务器发布清单无哈希差异，原始Word哈希未变。服务器备份为`.implementation_backups/before_smoke_v030_20260920T070004Z.tgz`。`runs/smoke_v030`尚未创建，新增真实模型调用0次。

第一次上传曾被自动审批按“服务器仅保留README”拦截，未传输；用户随后明确授权同步代码和README，才完成发布。另有一次服务器验证命令仅因reports目录不存在而未开始测试；创建日志目录后79项CPU测试正常完成。这两件事均未发起模型调用，也没有修改环境依赖。

## 7. Claude真实smoke完成后的独立审计

随后Claude完成`runs/smoke_v030`，状态`full_smoke_pass`、退出码0、非合成，77次调用全部完成、23条执行、278条事件、4个周期、782.614秒；输入31836/output23314 token。主实现者只读取回原始证据，没有追加调用、改变源码、重跑或启动主实验。

本地副本为`runs/smoke_v030/`；config/events/summary的SHA256与服务器一致。Journal哈希链、冻结源码哈希、调用先后、执行引用、新臂加入→执行→路由更新的顺序均独立核对。结果见`reports/smoke_v030_audit.json`。0.3.0发布清单及README保留为当时冻结版本，不回写历史运行或冒充运行前已经知道本次结果。

实际有6次生成，其中4个结构合法、2个因`deterministic node must have model=null`被正确拒绝；这不属于模型调用/工作流执行错误，不能把“错误事件0”说成“每次生成都有效”。3个候选实际接受评价，3个生成的新臂都获得了后续路由反馈。操作计划依次为Search+Deploy、Search+Deploy、Deploy+Deploy、Search+Deploy。

关键诊断：4次调度的`fallback`全部为false，`capped_at_one`全部为true，经验曲率均为1，ε约0.707–0.740，低于阈值0.8。9条曲率观测中8条条件边际为0，只有首条空集初始化边际为正。首个工作流在这两道搜索题上质量为1、成本奖励约0.4412，支配其余8个已评价点，因此后续点不增加HV。

用原始快照重算成对边际完全一致；对固定的已观测点集及最终档案快照分别计算HV总曲率，均为1。该有限实例即使把置信项设为0也仍是1，不能把本次问题仅归因于样本不足或通过缩小ε来制造非平凡曲率。最终档案详情和计算结果保存在审计JSON中。

预算比例约38.73%、38.04%、38.19%、39.97%；h始终为0.63212，因此比例变化来自线性估计/上下文，未覆盖曲率变化带来的适应。该结论约束的是本次两题、HV工程替代和冻结观测，不是导师未定义完整效用的总体曲率，也不否定整个idea。

## 8. 零新增模型调用的离线复核（2026-09-20）

用户授权“做必要检测，如果不消耗token的话你可以自己跑”。本次仅在本地CPU处理已有证据，新增模型推理调用0次、新增推理token为0；没有加载模型、连接服务器、启动48调用后续矩阵或主实验。这里的零token指实验模型，不包含对话助手本身。

新增独立审计入口`scripts/audit_smoke_offline.py`及结果`reports/smoke_v030_offline_checks.json`。脚本禁止网络、子进程及模型库导入；唯一后端按原始请求顺序读取已完成响应，请求不匹配或缓存耗尽即失败。临时重放日志自动删除，不能冒充新增真实运行。复核命令：

```bash
.venv/bin/python scripts/audit_smoke_offline.py
```

冻结的35份源码、任务文件、README、发布清单和原始运行证据均未改动；审计脚本为本地新增工具，没有修改0.3.0的冻结发布清单，也未上传服务器。

复核结果：

- 77/77请求的提示、参数、随机种子与模型标识完全匹配；生产策略从空状态消费缓存响应，重新完成相同4周期、278条事件。246条事件payload逐位相同，其余仅数值差异，最大绝对差为4.440892098500626e-15；事件类型、ID、选择和实际操作计划均一致。
- 原始23条最终输出经冻结评分器重新评分，与记录全部一致。12道任务的gold/private替换为哨兵后，公开输入完全不变；本轮全部为MATH train开发任务。这项检查不涵盖尚未使用的其他数据集环境。
- 独立数值计算复核3次搜索的RBF核、正半定性、6步logdet增益/贪心分数/胜出者；5次路由的双头OFUL预测、选择及实际评分/成本驱动的矩阵更新；4次调度的历史线性模型、带符号奖励、曲率、分配比例与累计取整；9条HV成对边际。均与日志一致。公开特征构造仍复用冻结代码，不声称这是完全独立的算法实现。
- 缓存不匹配、耗尽的两项负向检查均正确拒绝。第一次审计脚本运行因内存整数键与JSON字符串键的比较方式中止，修正为比较落盘JSON后通过；这是审计工具问题，未修改实验实现。

两个冻结状态下的数值对照补充了工程证据，但不是新实验或性能消融：保留原历史，仅把γ设0，则Search比例约49.27%–51.30%，累计计划为4 Search+4 Deploy；原计划为3 Search+5 Deploy，差异出现在第3周期。这说明曲率项确实连接到操作分配，不能证明真实运行中曲率变化的收益。每次路由在同一任务/臂集上把所有头重置，5次中4次胜出者改变，说明积累状态已影响选择；但实际5个胜出臂在被选前都没有自身路由更新，仍是早期探索，不能据此证明学会了任务匹配。

现有日志仅有2组“相同工作流+相同题目”的重复执行。其中`77aa4f58…`在`number_theory/train/99`上，`cycle:1:0:search:1:task:1`评分1，`cycle:3:0:deploy:execution`评分0；评分复算一致，采样种子不同。另1组两次皆对。应报告一次已观察到的答题翻转，不把它当作可靠翻转率，也不能继续把最初固定流程probe的0/12翻转推广到生成流程。

结论：本轮覆盖范围内未发现需要修复的评分、决策或记账不一致。经验曲率=1的退化仍成立；真实稳定性、扩大任务后的HV表现和路由收益不能由缓存重放补证，仍需另行确定最小模型预算。
