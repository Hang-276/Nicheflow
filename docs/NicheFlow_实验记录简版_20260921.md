# NicheFlow 实验记录

**记录范围：2026-09-20 至 09-21；最新运行证据截至 09-21 13:14:59（北京时间）。**

## 固定设置

- Qwen2.5-7B-Instruct-AWQ + `deepseek-flash`；只学习工作流、代理及在线决策参数，不微调底座。
- MATH 开发 35／校准 70；quick35 终评为 **35 题 × 3 组权重 × 1 样本 = 105 槽位**，终评不更新参数。
- 默认温度 0.7、输出上限 2048；本地 top_p=0.8、派生固定种子，API 不发送 top_p/seed。三组权重按质量／成本奖励排序。

## 改了什么及验证结果

| 版本 | 改动 | 实际验证 |
|---|---|---|
| v041 → v042 | 接入完整 MATH-500 后，增加 quick35 默认入口；按七科×五难度固定选题，保留 full500。 | quick/full 协议与冻结评测通过 CPU 检查；不能把模拟结果当实测。 |
| v042 → v043 | 修复单次 `length` 截断导致整批停止：改为质量 0、保留成本、停止后续节点，不重试；其他异常不按此规则放行。 | v042 在 86 次调用后停止、0 轮完成；v043 **完成 1 轮及 105 槽位终评**，899 次调用，约 94.4 分钟，API 核算 $0.1391。 |
| v043 → v044 | 修复生成器将 `Format` 写成 `model=null`：提示中明确算子执行类型、模型枚举、输入约束；保留严格校验。启动脚本增加显式轮数传递。 | 本地／服务器各 114 项 CPU 测试通过。真实链路检查 55 次调用、约 4.8 分钟、API 核算 $0.0176；两候选均合法，一个候选完成评价、入库并获得路由反馈；无截断或系统失败。 |

v044 链路检查只用开发 2／校准 2 题、1 轮、无终评，证明候选链路可执行，**不提供性能结论**。v043 的两次生成均因 Format 模型字段缺失被拒绝，未产生新候选精英；这是 v044 修复的直接依据。[变更记录][record] · [链路证据][chain]

v043 已知结果：按权重 (0.5,0.5)／(0.25,0.75)／(0.75,0.25)，正确数为 **25／32／22（各 35 题）**；全程 19 个工作流长度截断、其他执行错误 0。两份快照有限／正定，终评状态未更新。仅为 quick35 单次诊断，不能据此证明学习增益或权重优劣。[结果证据][v043]

## v044 六轮运行状态

- 运行：`main_v044_quick_r6`，09-21 12:15:29 启动；目标 **6 轮／12 操作块**，最后执行 105 槽位终评。
- 截点：初始化完成，保存 `round_0000`；**完整学习轮数 0**。第一轮生成 2 个合法候选，代理选中 1 个，仍在开发题评价；调用开始 622／完成 621，1 条在途，尚无本轮终评。
- 首次调度：搜索比例 0.3873，执行计划 `[search, deploy]`；γ=1、h=0.6321，来自上限截取，非置信半径超阈值回退。
- 硬上限：**6849 次调用／18 小时／$17 API**，不是实际用量或账单。旧工程记录中的“六轮未启动”已被上述运行证据更新。[截点证据][audit]

## 六轮实验结果 待填写

| 运行验收 | 实际结果 |
|---|---|
| 完成时间／完成轮数／终评完成槽位 |  |
| 总调用数／墙钟耗时／API 核算美元 |  |
| 有效候选数／新入库候选数／获得路由反馈的新候选数 |  |
| 参数健康／终评前后状态摘要一致 |  |

| 权重（质量，成本奖励） | 正确数／35 | Pass@1 | 平均核算成本 USD/题 | 长度截断／其他执行故障数 |
|---|---|---|---|---|
| (0.50, 0.50) |  |  |  |  |
| (0.25, 0.75) |  |  |  |  |
| (0.75, 0.25) |  |  |  |  |

截断／失败留在分母；三组权重分别统计，不合并为 Pass@3。平均核算成本包含 API 与本地推理时间核算，区别于 API 支出。

**结果观察与结论：**

<!-- 跑完后填写主要变化、异常及解释；有独立的初始化后评测时，才填写学习前后增益。 -->

## 结论与待办

**已完成：截断处理修复、一轮真实终评、候选生成修复及真实链路验证。六轮完成情况尚需后续日志确认。**三层主体保留，工程补充与导师差异见[详版][detail]。

下一步先核验六轮完成状态与冻结终评，再做初始化后／学习后的同协议比较；比较脚本已有曲率归集漏计、截断统计口径不一及配对校验不足，需先处理。当前没有基线／消融结果，不声称性能提升、统计收敛或理论保证成立。

[record]: ENGINEERING_RECORD.md
[chain]: ../reports/candidate_chain_v044/chain_check.json
[v043]: ../reports/main_v043_quick_final/summary.json
[audit]: ../reports/architecture_audit_20260921/audit_verification.json
[detail]: NicheFlow_本轮模型架构与实验设置_20260921.md
